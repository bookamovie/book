CREATE TABLE IF NOT EXISTS screens (screen INTEGER NOT NULL, cinema TEXT NOT NULL, location TEXT NOT NULL, seats INTEGER NOT NULL, PRIMARY KEY (screen, cinema, location));
CREATE TABLE IF NOT EXISTS bookings (id TEXT PRIMARY KEY, movie TEXT NOT NULL, screen INTEGER NOT NULL, seat INTEGER NOT NULL, date TEXT NOT NULL, cinema TEXT NOT NULL, location TEXT NOT NULL);